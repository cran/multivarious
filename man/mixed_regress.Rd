% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mixed_regress.R
\name{mixed_regress}
\alias{mixed_regress}
\title{Mixed-effect multivariate regression}
\usage{
mixed_regress(
  Y,
  design = NULL,
  fixed,
  random = NULL,
  basis = identity_basis(),
  preproc = center(),
  term_scopes = NULL,
  exchangeability = NULL,
  ...
)
}
\arguments{
\item{Y}{Response matrix (\verb{n_obs x p}) or 3D array (\verb{n_subject x n_within x p}).}

\item{design}{Optional design data frame. Required for matrix input.}

\item{fixed}{Fixed-effect formula.}

\item{random}{Random-effect specification. Either \code{NULL} (fixed-effects only)
or a one-sided formula of the form \code{~ ... | group} understood by
\code{reformulas::findbars()} / \code{lme4::findbars()}. All random-effects bars
must share a single grouping variable.}

\item{basis}{Feature basis specification or projector-like object.}

\item{preproc}{Response preprocessor.}

\item{term_scopes}{Optional named character vector or named list overriding
automatic effect-scope classification for fixed-effect terms. Allowed values
are \code{"between"}, \code{"within"}, \code{"mixed"}, and \code{"ungrouped"}.}

\item{exchangeability}{Optional named character vector or named list
specifying permutation/block-resampling structure for fixed-effect terms.
Allowed values are \code{"between_subject"}, \code{"within_subject"},
\code{"whole_subject"}, and \code{"rows"}. When omitted, exchangeability is inferred
from grouped-design scope.}

\item{...}{Reserved for future extensions.}
}
\value{
A \code{mixed_fit} object.
}
\description{
Fit the row-side design geometry for operator-valued ANOVA. Supports
fixed-effect designs (\code{random = NULL}) and grouped random-effects designs
with a single grouping variable (random intercept and optional random
slopes, shared-\code{Omega} across response features).
}
