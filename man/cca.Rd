% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cca.R
\name{cca}
\alias{cca}
\title{Canonical Correlation Analysis (CCA)}
\usage{
cca(
  X,
  Y,
  ncomp = NULL,
  preproc_x = center(),
  preproc_y = center(),
  lambda = 1e-04,
  lambda_x = lambda,
  lambda_y = lambda,
  tol = sqrt(.Machine$double.eps),
  ...
)
}
\arguments{
\item{X}{Numeric matrix of predictors (n x p_x).}

\item{Y}{Numeric matrix of outcomes (n x p_y). Must have the same number of
rows as \code{X}.}

\item{ncomp}{Number of canonical dimensions to return. Defaults to
\code{min(ncol(X), ncol(Y), nrow(X) - 1)} after preprocessing.}

\item{preproc_x}{Preprocessor for the X block (default: \code{center()}).}

\item{preproc_y}{Preprocessor for the Y block (default: \code{center()}).}

\item{lambda}{Shared ridge shrinkage level used when \code{lambda_x} and
\code{lambda_y} are not supplied. The effective ridge added to each block
is \code{lambda * mean(diag(S))}, where \code{S} is the block covariance.}

\item{lambda_x}{Ridge shrinkage level for the X block covariance.}

\item{lambda_y}{Ridge shrinkage level for the Y block covariance.}

\item{tol}{Eigenvalue floor used when whitening regularized covariance
matrices. Defaults to \code{sqrt(.Machine$double.eps)}.}

\item{...}{Extra arguments stored on the returned object.}
}
\value{
A \code{cross_projector} with class \code{"cca"} containing
canonical coefficients (\code{vx}, \code{vy}), block scores (\code{sx},
\code{sy}), canonical correlations (\code{cor}), and the regularized block
covariance matrices used to fit the model.
}
\description{
Reference implementation of two-block canonical correlation analysis that
returns a \code{cross_projector}. The within-block covariance matrices are
ridge-regularized before whitening, which keeps the fit well-defined even
when \eqn{p > n} or either block is rank-deficient.
}
\examples{
set.seed(1)
X <- matrix(rnorm(120), 30, 4)
Y <- matrix(rnorm(90), 30, 3)
fit <- cca(X, Y, ncomp = 2)
fit$cor
}
